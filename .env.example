# Canonical public URL used for sitemap, RSS, and social metadata.
SITE_URL=https://your-project.vercel.app

# Neon pooled connection string. Configure this in Vercel Project Settings
# or through the Neon integration. Never commit a real credential.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
